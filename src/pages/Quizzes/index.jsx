import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
import "./Quizzes.css";

const Quizzes = () => {

    return(
        <h1>hi</h1>
    );
};

export default Quizzes;
